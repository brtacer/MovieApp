server:
  port: 8085

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: 1316
    url: jdbc:postgresql://localhost:5432/movieapp
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

